import React from 'react';

import HeaderCard from '../cards/HeaderCard';

const SettingsHeaderContainer: React.VoidFunctionComponent = () => {
  return (
    <div className="SettingsHeaderContainer">
      <HeaderCard text={'Settings'} />
    </div>
  );
};

export default SettingsHeaderContainer;
